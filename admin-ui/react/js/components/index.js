/**
 * Components Index
 * 
 * Loads all available components for the Advanced Settings plugin.
 * This file serves as the entry point for all UI components.
 */

// Initialize components object if it doesn't exist
window.AdvSetComponents = window.AdvSetComponents || {};

// Note: We're not using import statements here because they don't work directly in the browser.
// The components are loaded in the correct order via script tags in admin-ui.js.

// This file is intentionally minimal as it's just a placeholder for the component registry.
// Individual components are loaded separately and register themselves with the registry. 